extends layout

block content
  h1 Find Your Train - this change should be on new branch
  p Select your train below to see its current schedule. made another change on the new branch.
  #wrapper
    #trainList
      h2 Trains
      #trains.d-flex.flex-row
    #trainInfo
      strong  <span id='trainName'></span>
      #trainSchedule
        strong Select a train to view its current schedule.